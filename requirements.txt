pandas>=2.0,<3.0
numpy>=1.26
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
lightgbm>=4.0,<5.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
pyarrow>=15.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.0,<3.0
requests>=2.31,<3.0
joblib>=1.3,<2.0
python-dotenv>=1.0,<2.0
imbalanced-learn>=0.12,<1.0

# PyTorch intentionally excluded until Phase 3 gate:
#   ensemble F1 >= 0.70 on rain occurrence AND MAE < 1.2mm on non-zero rainfall
# Only add torch / pytorch-forecasting if that gate is NOT reached.
